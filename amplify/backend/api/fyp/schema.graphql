type PlanModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  type: Boolean
  MobileMinutes: Int
  MobileRate: Float
  LandlineMinutes: Int
  LandlineRate: Float
  IntMinutes: Int
  IntRate: Float
  PremMinutes: Int
  PremRate: Float
  CostPerMonth: Int
}
